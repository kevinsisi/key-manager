## 1. Server model and summary

- [x] 1.1 Normalize legacy key statuses before building summaries.
- [x] 1.2 Build quota-bucket-aware summary with `scoped / mixed / unscoped` trust states.
- [x] 1.3 Extend `/api/keys/quota-summary` and `/api/keys/export` with trusted/untrusted outputs.

## 2. Web UI

- [x] 2.1 Surface trusted key/bucket counts and warning messages in the dashboard.
- [x] 2.2 Make quota bucket tagging explicit in Add/Edit/Batch import flows.

## 3. Verification and handoff

- [x] 3.1 Run concrete build/lint checks.
- [x] 3.2 Verify API responses against live/local expectations.
- [ ] 3.3 Update memory/docs, commit, and push.
